@model ProjectManagementSystem.ViewModels.CustomerViewModel

@{
    ViewData["Title"] = "Delete Customer";
}

<div class="container mt-4">
    <h2>Delete Customer</h2>
    <div class="alert alert-danger mt-3">
        <h4>Are you sure you want to delete this customer?</h4>
    </div>

    <div class="card mt-3">
        <div class="card-body">
            <dl class="row">
                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Name)</dt>
                <dd class="col-sm-9">@Model.Name</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.Email)</dt>
                <dd class="col-sm-9">@Model.Email</dd>

                <dt class="col-sm-3">@Html.DisplayNameFor(model => model.PhoneNumber)</dt>
                <dd class="col-sm-9">@Model.PhoneNumber</dd>

                <dt class="col-sm-3">Number of Projects</dt>
                <dd class="col-sm-9">@Model.ProjectCount</dd>
            </dl>
        </div>
    </div>

    <form asp-action="Delete" class="mt-3">
        <input type="hidden" asp-for="Id" />
        <input type="submit" value="Delete" class="btn btn-danger" />
        <a asp-action="Index" class="btn btn-secondary">Back to List</a>
    </form>
</div>